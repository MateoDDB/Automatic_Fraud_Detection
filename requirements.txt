# Dépendances applicatives (entraînement local + tâches exécutées dans les DAGs).
# Apache Airflow n'est pas listé ici : il provient de l'image Docker officielle,
# installée avec son fichier de contraintes (voir docker-compose.yml).
# Versions épinglées pour Python 3.11.

pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
joblib==1.4.2
requests==2.32.3
psycopg2-binary==2.9.9
python-dotenv==1.0.1
