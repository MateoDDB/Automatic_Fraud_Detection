# Dépendances du dashboard Streamlit, dans une image dédiée et séparée de l'image
# Airflow afin de ne pas perturber la parité des versions du modèle (scikit-learn,
# joblib). Le dashboard ne charge pas le modèle : il lit seulement la base Neon.
pandas==2.2.2
numpy==1.26.4
psycopg2-binary==2.9.9
python-dotenv==1.0.1
streamlit==1.40.2
plotly==5.24.1
